# إصلاح سرعة إضافة قطع الغيار في الورشة

## السبب الحقيقي
واجهة جدول قطع الغيار كانت تعرض البيانات من `partUsages` داخل `useRepairPartUsages()`، بينما زر الإضافة كان يعدّل نسخة أخرى داخل `localStorage/db` فقط، ثم يطلق حدثًا عامًا يجبر الـHook على إعادة الجلب من Supabase. لذلك الصف لم يكن يظهر فورًا، وأحيانًا كانت البيانات البعيدة القديمة تكتب فوق التعديل المحلي.

كذلك تحديث المخزون عبر `updateProduct()` كان ينتظر Supabase قبل تحديث React state.

## التعديلات
- إضافة `updateProductLocal()` لتحديث رصيد المنتج في React ونسخة الكاش فورًا دون انتظار الشبكة.
- إضافة `replacePartUsagesLocal()` لتحديث نفس الـstate الذي يرسم جدول الورشة فورًا.
- منع إطلاق أحداث إعادة الجلب العامة قبل نجاح الحفظ الخلفي.
- جعل طلبات Supabase الأساسية `await` داخل كتلة الحفظ حتى تدخل الأخطاء في مسار الـrollback بدل تجاهلها.
- عند فشل الحفظ: استرجاع القطعة والكمية والأوردر محليًا.

## الملفات المعدلة
- `src/components/RepairCenter.tsx`
- `src/hooks/useData.ts`

## الاختبار المطلوب
1. افتح أوردر في الورشة.
2. اضغط على قطعة متوافقة مرة واحدة.
3. يجب أن يظهر الصف فورًا في جدول القطع.
4. انتظر اكتمال مؤشر الحفظ، ثم اعمل Refresh.
5. يجب أن تظل القطعة والكمية محفوظتين.
6. جرّب `+` و`-` والحذف، وتأكد من رجوع المخزون.

## ملاحظة التحقق
تعذر تثبيت الحزم وتشغيل Build كامل داخل بيئة المراجعة لأن Registry الداخلي لا يحتوي `@google/genai`. تم فحص الملفين المعدلين عبر TypeScript transpilation ولم تظهر أخطاء Syntax.
