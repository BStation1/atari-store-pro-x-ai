# فحص وإصلاح المحاسبة — Atari Store Pro X

## السبب الجذري

المشروع كان يحتوي على أكثر من مسار مستقل لحساب تكلفة قطع الغيار وصافي الربح. بعض الشاشات كانت تستخدم `device.partsCost`، وهو سعر بيع القطع للعميل، على أنه تكلفة شراء. لذلك كانت تكلفة البضاعة تساوي الإيراد أحيانًا، بينما شاشات أخرى تعرض أرقامًا مختلفة أو صفرًا.

## الإصلاح المنفذ

تمت إضافة `src/lib/accountingEngineV2.ts` كمصدر موحد لحسابات أوامر الصيانة.

ترتيب استخراج تكلفة الشراء:

1. حركات المخزن المرتبطة بالأوردر، بسعر الشراء المسجل وقت السحب.
2. سجلات استخدام قطع الغيار المرتبطة بالأوردر.
3. عناصر الصيانة القديمة التي تحتوي على تكلفة شراء صريحة، ومنها:
   - `selectedRepairItems[].costPrice`
   - `repairItems[].purchaseCost`
   - `items` و`parts` مع حقول تكلفة شراء صريحة.
4. حقول تكلفة شراء صريحة على الجهاز مثل `partsPurchaseCost`.

لا يستخدم المحرك `device.partsCost` أو سعر البيع أو إجمالي الفاتورة كتـكلفة شراء.

## قواعد التوزيع

- شغل المحل: صافي الربح 50% لأحمد و50% لعبده.
- شغل أحمد: صافي الربح 100% لأحمد.
- شغل عبده: 25% من صافي الربح لأحمد و75% لعبده.
- المستحق لأحمد على عبده يساوي نصيب أحمد 25% فقط.

## الملفات التي تم توحيدها على المحرك الجديد

- `src/components/Accounting.tsx`
- `src/components/partner-accounting/ProfitsSummary.tsx`
- `src/components/financial-reports/ShopProfitsReportView.tsx`
- `src/components/financial-reports/AhmedDashboardView.tsx`
- `src/components/financial-reports/AbdoDashboardView.tsx`
- `src/lib/dashboard/dashboardMetrics.ts`
- المسار الرئيسي للأرباح داخل `src/lib/analytics/analyticsMetrics.ts`

## اختبار القواعد

بيانات الاختبار:

- المحل: إيراد 2000، تكلفة شراء 300.
- أحمد: إيراد 4000، تكلفة شراء 400.
- عبده: إيراد 4800، تكلفة شراء 600.

النتيجة:

- إجمالي الإيرادات: 10800.
- إجمالي تكلفة الشراء: 1300.
- صافي الربح: 9500.
- نصيب أحمد: 5500.
- نصيب عبده: 4000.
- المستحق لأحمد على عبده: 1050.

تم تنفيذ الاختبار البرمجي ونجح بهذه القيم.

## ملاحظات الفحص

- ملفات الورشة والمخزون لم تُعدّل.
- تعذر تشغيل `npm install` في بيئة الفحص لأن مستودع الحزم الداخلي لا يوفر `@google/genai`.
- تم فحص تركيب TypeScript/TSX للملفات المعدلة بلا أخطاء تركيبية.
