# محرك المحاسبة V2 — المرحلة الأولى

تم تنفيذ مصدر حساب واحد داخل:

- `src/lib/accountingEngineV2.ts`

وتم ربط شاشة محاسبة الشركاء به داخل:

- `src/components/partner-accounting/ProfitsSummary.tsx`

## القواعد المثبتة

- شغل المحل: صافي الربح 50% أحمد / 50% عبده.
- شغل أحمد: صافي الربح 100% أحمد.
- شغل عبده: صافي الربح 25% أحمد / 75% عبده.
- المستحق لأحمد على عبده = نصيب أحمد 25% فقط.

## ترتيب تكلفة الشراء

1. حركات المخزن الخارجة المرتبطة بالأوردر.
2. سجلات استخدام قطع الغيار المرتبطة بالأوردر.
3. حقول تكلفة شراء صريحة على الجهاز.
4. `partsCost` للبيانات القديمة فقط إذا لم توجد حركات أو استخدامات مرتبطة.

## منع اختلاف الكروت والجدول

الكروت، جدول الأوردرات، تسوية عبده، وتقرير البضاعة المسحوبة داخل `ProfitsSummary` أصبحت تعتمد على صفوف المحرك نفسها بدل حساب كل جزء بصورة مستقلة.

## اختبار القواعد الثابتة

- المحل: 2000 - 300 = 1700؛ أحمد 850؛ عبده 850.
- أحمد: 4000 - 400 = 3600؛ أحمد 3600؛ عبده 0.
- عبده: 6000 - 600 = 5400؛ أحمد 1350؛ عبده 4050؛ المستحق 1350.

## ملاحظة التحقق

تم فحص TypeScript للملفات المعدلة. لم تظهر أخطاء تركيبية في الملفات الجديدة أو المعدلة؛ تعذر تشغيل البناء الكامل فقط لأن بيئة الحزم الحالية لا توفر `@google/genai`.
