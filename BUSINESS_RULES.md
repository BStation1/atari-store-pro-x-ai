# Atari Store Pro X — Official Business Rules

هذا الملف هو المرجع الرسمي لقواعد العمل والمحاسبة داخل المشروع. أي تعديل برمجي أو بواسطة أدوات الذكاء الاصطناعي يجب أن يلتزم بهذه القواعد، ولا يجوز تغييرها دون موافقة مالك النظام.

## 1. سياسة الفروع والنشر

- `main`: النسخة الرسمية المنشورة للمستخدمين. لا يتم التعديل عليها مباشرة.
- `develop`: نسخة تجميع واختبار التعديلات المقبولة.
- كل إصلاح أو ميزة جديدة يجب أن تُنفّذ على فرع مستقل.
- يجب اختبار كل تعديل على Vercel Preview قبل دمجه.
- لا يتم الدمج إلى `main` إلا بعد نجاح الاختبارات اليدوية ونجاح البناء.

## 2. تعريف أنواع الشغل

### شغل المحل — SHOP

- الإيراد يُسجّل ضمن إيرادات المحل.
- صافي الربح يُقسم بالتساوي:
  - أحمد: 50% من صافي الربح.
  - عبده: 50% من صافي الربح.

### شغل أحمد — AHMED

- أحمد يحصل على 100% من صافي الربح.
- عبده لا يحصل على نسبة من شغل أحمد.
- إيراد شغل أحمد الخاص لا يدخل ضمن إجمالي إيرادات فلتر «الجميع».
- تكلفة البضاعة المستخدمة في شغل أحمد تدخل ضمن إجمالي البضاعة المسحوبة.

### شغل عبده — ABDO

- الإيراد يُسجّل ضمن الإيرادات المعروضة للمحل/عبده.
- صافي الربح يُقسم:
  - أحمد: 25% من صافي الربح.
  - عبده: 75% من صافي الربح.
- المستحق على عبده يساوي:
  - تكلفة البضاعة المسحوبة في شغل عبده.
  - بالإضافة إلى نصيب أحمد، وهو 25% من صافي ربح شغل عبده.

## 3. المعادلات الأساسية

- صافي الربح = إجمالي الإيراد بعد الخصم − تكلفة شراء البضاعة المستخدمة.
- تكلفة البضاعة تعتمد على سعر الشراء، وليس سعر البيع.
- شغل المحل:
  - نصيب أحمد = صافي الربح × 50%.
  - نصيب عبده = صافي الربح × 50%.
- شغل عبده:
  - نصيب أحمد = صافي الربح × 25%.
  - نصيب عبده = صافي الربح × 75%.
  - المستحق على عبده = تكلفة البضاعة المسحوبة + نصيب أحمد.
- شغل أحمد:
  - نصيب أحمد = صافي الربح.
  - نصيب عبده = صفر.

## 4. قواعد فلتر «الجميع»

عند اختيار «الجميع» في محاسبة الشركاء:

- إجمالي الإيرادات = إيرادات شغل المحل + إيرادات شغل عبده فقط.
- إيرادات شغل أحمد الخاص لا تدخل ضمن إجمالي الإيرادات.
- إجمالي البضاعة المسحوبة = بضاعة شغل المحل + بضاعة شغل أحمد + بضاعة شغل عبده.
- إجمالي مستحق أحمد = 50% من صافي ربح المحل + 25% من صافي ربح عبده.
- إجمالي مستحق عبده = 50% من صافي ربح المحل + 75% من صافي ربح عبده.
- المستحق على عبده = تكلفة بضاعة شغل عبده + 25% من صافي ربح عبده.

## 5. المخزون وقطع غيار الصيانة

- عند إضافة قطعة لأوردر صيانة، تُخصم الكمية مرة واحدة فقط.
- عند حذف أو إرجاع قطعة، تُعاد الكمية مرة واحدة فقط.
- لا يُسمح للمخزون أن يصبح سالبًا.
- لا يتم دمج قطعتين مختلفتين في سطر واحد.
- يجب أن تظل جميع القطع ظاهرة بعد إغلاق الأوردر وإعادة فتحه.
- سجلات `Repair Part Usages` المحفوظة في Supabase هي المصدر الأساسي لقطع الصيانة.
- الحالة المحلية أو `selectedRepairItems` ليست مصدر الحقيقة، وتستخدم للعرض أو كحل احتياطي للبيانات القديمة فقط.
- القطع بالحالة `RETURNED` أو `REVERSED` أو `CANCELLED` لا تدخل في التكلفة أو الإيصال.

## 6. الأسعار والأرباح

- سعر بيع القطعة منفصل عن سعر شرائها.
- إجمالي بيع القطع = مجموع سعر البيع × الكمية.
- إجمالي تكلفة القطع = مجموع سعر الشراء × الكمية.
- لا يتم تسجيل تكلفة القطعة كخسارة مستقلة ثم خصمها مرة أخرى من الربح.
- لا تُحسب أرباح نهائية محققة لأوردر ملغي أو محذوف.
- الخصومات تقلل الإيراد قبل حساب صافي الربح.
- القيمة صفر قيمة صحيحة، ولا يجوز استبدالها بقيمة fallback لمجرد أنها falsy.

## 7. الحذف والإرجاع

- حذف أوردر صيانة يجب أن يعيد القطع الفعالة للمخزون مرة واحدة.
- يجب عكس أو إلغاء القيود المحاسبية المرتبطة بالأوردر المحذوف.
- لا يجوز حذف أوردر مسلّم ومغلق دون إجراء إداري واضح لعكس التسليم أولًا.
- المرتجعات يجب أن تلغي تكلفة الكمية المرتجعة من حساب الأرباح.

## 8. منع التكرار

- لا يتم إنشاء قيد مالي أو حركة مخزون مكررة لنفس العملية.
- يجب استخدام معرّف ثابت أو `idempotencyKey` عندما تتوفر عمليات قابلة للتكرار.
- أحداث المزامنة هدفها إعادة جلب البيانات فقط، ولا يجب أن تعيد تنفيذ الخصم أو العملية المالية.

## 9. الاختبارات الإلزامية قبل الدمج

يجب اختبار الحالات التالية على Preview:

- شغل المحل: أحمد 50% وعبده 50%.
- شغل أحمد: أحمد 100% وعبده صفر.
- شغل عبده: أحمد 25% وعبده 75%.
- المستحق على عبده = تكلفة بضاعة عبده + نصيب أحمد من شغل عبده.
- فلتر «الجميع» يطابق القواعد المذكورة أعلاه.
- إضافة قطعتين مختلفتين وظهورهما بعد إعادة فتح الأوردر.
- حذف قطعة وإعادة كميتها للمخزون مرة واحدة.
- الخصم والمرتجع لا يؤديان إلى أرقام مكررة أو سالبة غير صحيحة.

## 10. تعليمات أدوات الذكاء الاصطناعي والمطورين

- اقرأ هذا الملف قبل تعديل منطق المحاسبة أو المخزون أو الصيانة.
- لا تعِد بناء المشروع من الصفر.
- لا تغيّر أكثر من الملفات المرتبطة مباشرة بالمشكلة.
- لا تعدّل `main` مباشرة.
- لا تعتبر نجاح Build دليلًا كافيًا على صحة الحسابات؛ يجب اختبار النتائج المالية يدويًا على Preview.
- عند وجود تعارض بين الكود الحالي وهذا الملف، تُعتبر قواعد هذا الملف هي المرجع المطلوب، ويجب مراجعة مالك النظام قبل تغييرها.
